package com.xavier.practice.concurrent.semaphore.service;

import java.util.concurrent.Semaphore;

public class SemaphoreOne extends BaseSemaphore {
    public SemaphoreOne(Semaphore semaphore) {
        super(semaphore);
    }
}
